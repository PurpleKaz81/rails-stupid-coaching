<%= Time.now.strftime("%A, %B %d, %Y %l:%M %p") %> <br>

<%= form_tag(ask_path, method: :get, 'data-turbo': false) do %>
  <%= text_field_tag :question, nil, placeholder: "Ask a question" %>
  <%= submit_tag "Ask!" %>
<% end %>

<% if @answer.present? %>
  <p><%= @answer %></p>
<% end %>
